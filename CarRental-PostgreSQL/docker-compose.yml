version: '3.1'

services:
  db:
    image: postgres:18
    restart: always
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: dbuser
      POSTGRES_PASSWORD: dbpassword
    ports:
      - "5433:5432"
    volumes:
      - pg:/var/lib/postgresql/data
volumes:
  pg: